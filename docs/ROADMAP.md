# BotFlow — план развития
Статус: актуальный roadmap после product discovery 26–27.08.2026.

Главный принцип: эволюция без большого переписывания. Каждый этап делится на небольшие проверяемые изменения и не начинается, пока предыдущий не имеет зелёных проверок.

## Приоритет решений

1. Корректная оплата и гарантированная выдача.
2. Честные права публикации и жизненный цикл бота.
3. Понятный путь от Welcome до работающего бота.
4. Единая рабочая область аккаунта и бота.
5. Удобство сценария, аудитории и аналитики.
6. Визуальная полировка.
7. Будущие платформы и сценарии.

## R0. Документация и точка отсчёта

Статус: выполнено и зафиксировано в `7ec4ca0`.

- зафиксировать глоссарий;
- закрепить новую продуктовую модель;
- отделить current runtime от target architecture;
- создать актуальную дизайн-систему;
- объединить эксплуатацию и release-check;
- пометить прежние документы как архив;
- исключить слоты, lifetime, общий PRO и trial из активной документации.

Gate:

- в активных документах нет противоречащих моделей;
- README ведёт только на актуальные документы;
- архив явно помечен и не используется как SSOT.

## R1. Стабилизация текущей frontend-базы

Статус: выполнено. Реализация зафиксирована в `6fd7179` и `f684af4`; ручной
smoke в авторизованном Telegram WebView на desktop и телефоне пройден 27.08.2026.
Локальный Playwright не получает валидный Telegram `initData`, поэтому не может
честно проверить workspace-сценарии.

Проблема: в рабочем дереве уже находится крупная незакоммиченная перестройка shell/роутинга/UI. Продолжать поверх неё без ревью рискованно.

### Что сделать

1. Сопоставить каждый новый экран с существующей работающей функцией.
2. Вернуть достижимость реализованных сценария, Dashboard с клиентами и аналитикой, настроек и админки.
   Рассылки не маскировать пустым экраном: их отдельный workflow и API остаются задачей R7.
3. Проверить guard несохранённых изменений и переключение ботов.
4. Удалить только доказанно осиротевшие компоненты после проверки импортов.
5. Исправить текущие lint/TypeScript/test failures.
6. Провести browser smoke на desktop и Telegram mobile viewport.

### Результат

Стабильная исходная точка, на которой существующая функциональность не потеряна, а новая IA ещё не обещает отсутствующую backend-логику.

Gate: lint, build, pytest, browser smoke — зелёные. Отдельный commit.

## R2. Контракты домена и миграционный каркас

Статус: первый малый шаг выполнен. План совместимой миграции зафиксирован в
`docs/R2_MIGRATION_PLAN.md`; добавлены nullable migration и compatibility
контракты. Runtime-поведение пока не менялось.

### Backend

- добавить явный `scenario_type` для бота без удаления текущей funnel schema;
- формализовать lifecycle `draft/ready/published/paused/archived` и причину паузы;
- определить versioned payload сценария;
- ввести account-level `GatewayConnection`;
- добавить ссылку бота на одну активную кассу;
- подготовить `BotSubscription` и каталог возможностей;
- обеспечить совместимость существующих ботов через backfill.

### API

- DTO не раскрывают секреты;
- readiness возвращает машинно-читаемые причины;
- API различает текущую реализацию и будущие disabled capabilities;
- OpenAPI-контракты покрываются тестами.

Gate: миграции вперёд/назад в тестовом контуре, backfill, unit/integration tests.

## R3. Черновик, расчёт цены и публикация

### Поведение

- черновиков может быть несколько;
- внешний пользователь не проходит неопубликованный бот;
- владелец может тестировать свой черновик;
- конфигуратор рассчитывает цену конкретного бота;
- первая платформа включена, дополнительные увеличивают цену;
- trial отсутствует;
- публикация требует оплаты этого бота или подходящего Gift Grant;
- окончание подписки ставит только этот бот на паузу.

### Сервисы

- `BotPricingService` — детерминированный расчёт конфигурации;
- `BotEntitlementService` — право публикации конкретного бота;
- `BotLifecycleService` — единый запуск/пауза/возобновление;
- серверный `ReadinessService` остаётся источником истины.

Gate: матрица прав, повторная оплата, истечение, возобновление и изоляция нескольких ботов.

## R4. Первый вход и рабочие области

### Account Workspace

- Welcome Screen только при отсутствии ботов;
- «Мои боты» без искусственного счётчика слотов;
- библиотека касс;
- платежи за BotFlow;
- профиль и тема.

### Bot Workspace

- Обзор;
- Сценарий;
- Продажи;
- Аудитория;
- Аналитика;
- Настройки;
- полноценный Bot Context Switcher.

### Создание

Имя → сценарий → платформы/возможности → live-цена → бесплатный черновик или оплата. Токен запрашивается при подготовке публикации, а не как первый барьер.

Gate: новый пользователь, пользователь с одним/несколькими ботами, gift-flow, mobile keyboard/safe-area.

## R5. Воронка продаж

### Модель

- режимы `autopay`, `application`, `hybrid`;
- старт;
- дожимы 0–5;
- предложения с индивидуальной выдачей;
- единый media attachment;
- сохранение только при реальном diff;
- версия сценария для создаваемого счёта.

### UX

- последовательность видна без отдельной декоративной схемы;
- длинные тексты удобно редактировать на телефоне;
- фокус не скрывается под клавиатурой/navigation;
- tariff/offer-секция не превращается в бесконечный скролл;
- ошибки readiness ведут к конкретному полю.

Gate: все режимы, 0/1/5 дожимов, один/несколько offers, media replacement, token replacement, dirty-state.

## R6. Payment & Fulfillment Reliability

### Общий контракт провайдеров

- create payment;
- verify credentials;
- verify webhook;
- fetch authoritative status;
- normalize failed/canceled/succeeded;
- capability flags для чеков, рассрочки и возвратов.

### Надёжность

- immutable PaymentOrder snapshot;
- webhook маршрутизируется по Gateway Connection;
- idempotency для webhook и выдачи;
- старые счета используют старую кассу;
- повторная обработка безопасна;
- владелец получает обязательное уведомление через главный BotFlow;
- лид получает понятную ошибку без технических деталей;
- Dashboard обновляется только подтверждёнными данными.

Проверить отдельно YooKassa, Robokassa и Prodamus. Непроверенный провайдер скрывается или явно маркируется beta, но не объявляется равноправно рабочим.

Gate: реальные sandbox/contract tests и сценарии с повторными webhooks.

## R7. Аудитория и рассылки

- аудитория конкретного бота;
- лид определяется по `bot × platform × external identity`;
- фильтры все/оплатившие/неоплатившие;
- создание рассылки из Audience hub;
- одна платформа выбирается автоматически;
- несколько — только из оплаченных и подключённых;
- draft/scheduled/sending/sent/failed;
- батчи, повторяемость и защита от дублей.

Gate: корректный подсчёт получателей, отмена scheduled, повтор failed, изоляция ботов.

## R8. Достоверная аналитика

- дневные агрегаты по боту и платформе;
- посетители, продажи, выручка и конверсия;
- временной график;
- воронка потерь;
- loading/empty/error/stale раздельны;
- никакие отсутствующие данные не показываются как `0`.

Gate: reconciliation с платежами и лидами, timezone Europe/Moscow, диапазоны дат.

## R9. Административные подарки

- Gift Link и Gift Grant;
- конфигурация подарка, TTL, количество активаций;
- preview результата до генерации;
- активация аккаунтом;
- расходование после сохранения подходящего бота;
- доплата за возможности сверх подарка;
- отзыв и audit log.

Gate: повторное открытие, исчерпанный лимит, истёкшая/отозванная ссылка, конкурентная активация.

## R10. Design System rollout

Дизайн внедряется через реальные экраны, а не отдельной массовой заменой классов:

1. shell и навигация;
2. Welcome/Create Bot;
3. Bot Overview;
4. Scenario Editor;
5. Sales/Payments;
6. Audience;
7. Account Settings;
8. Admin.

На каждом экране сначала Product/UX review, затем visual review, извлечение повторяющихся компонентов и browser QA.

## R11. Будущие платформы и сценарии

После стабилизации v1:

- Platform Adapter contract;
- VK;
- MAX;
- «Запись на услугу» как отдельный scenario type;
- Mini App как отдельный scenario type;
- AI-автоответчик и пакеты ресурсов только после продуктового discovery.

## Правила выполнения roadmap

- один логический commit — одна проверяемая причина изменения;
- описание commit на русском;
- миграции и API меняются раньше зависимого UI;
- новый UI не скрывает существующую функцию без продуктового решения;
- крупная несвязанная находка фиксируется в roadmap, но не расширяет текущий PR;
- нельзя переходить к следующему этапу при красном gate;
- после этапа: изменения, проверки, ручной QA, риски и оценка готовности.
