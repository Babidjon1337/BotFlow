# BotFlow — план выполнения (исполнимый)

Статус: активный рабочий план. Правило: делаю пункт → проверяю (lint/build/pytest) → отмечаю чек-бокс здесь → двигаюсь дальше, без ожидания подтверждений. Все коммиты на русском.

Канвас эталона: https://superdesign.dev/teams/6675c740-cd8d-459e-802d-62c755bb513e/projects/82887146-7ac4-44c4-b27d-c4b4a2fc0b0f

---

## ФАЗА A — баги и структура разделов (по скринам 31.08)

### A1. ✅ Модель подписки: PRO и лицензии убраны из интерфейса — `528522f`
- ✅ Subscription: одна карточка «Подписка бота», hero — реальные дата/цена
- ✅ Profile: баннер «Подписка бота 990 ₽/мес», купленные базовые боты удалены
- ✅ Sidebar: «Подписка до…» вместо «PRO до…»
- ✅ AdminStats: колонки лицензий/PRO из таблицы убраны

### A2. ✅ Создание бота: прозрачная цена — `8ec68e6`
- ✅ База 990 ₽/мес, доп. платформа +500 ₽/мес, «оплата сразу или позже»
- ✅ Mini App — «дороже, цена при запуске»

### A3. ✅ Первый вход: сценарий без START — `e127210`
- ✅ Блок-заглушка «Остался один шаг» в Сценарии удалён
- ✅ Токен в настройках сохраняется без платёжки

### A4. ✅ Интеграции: единый раздел — `e4b1271`
- ✅ Платформы + Касса + Подключение START + Оферта в одном разделе
- ✅ Аудитория и Рассылки — раздельные разделы

### A5. 🔲 БАГ: вкладки «Аудитория/Рассылки» не переключаются
- **Симптом (скрин юзера):** обе вкладки на месте, клик не переключает/сжимает кнопки
- **Причина:** в App.tsx создаются ДВА экземпляра BroadcastsScreen (`key="audience"` и `key="broadcasts"`), у каждого свой локальный `tab`, и initialTab не применяется при смене view
- **Фикс:**
  - [ ] BroadcastsScreen: убрать внутренние табы вообще — «Аудитория» и «Рассылки» теперь РАЗНЫЕ разделы маршрута (A4), вкладки внутри не нужны
  - [ ] App.tsx: один экран, начальная вкладка из view (`audience` → аудитор-таб, `broadcasts` → рассылки-таб)
- **Проверка:** клик по разделам меняет контент, размеры табов не «плывут»

### A6. 🔲 Тон «Аудитории» — сменить (был = оранжевый Рассылок)
- [ ] AppShell BOT_VIEW_TONES: `audience: 'nav-tone-indigo'` (Рассылки остаются orange)
- [ ] Проверка: оба раздела различимы цветом в меню

### A7. 🔲 Интеграции: касса — видимые лого платёжек БЕЗ аккордеона
- **От юзера:** «неудобно что кассу нужно разворачивать, должны быть иконки, видно лого платежек»
- [ ] Секция «Касса» всегда открыта (не аккордеон)
- [ ] Карточки касс видны сразу: лого (yookassa/robokassa/prodamus PNG из constants) крупно, название, статус
- [ ] При наличии подключённой кассы — карточки приглушены, активная выделена «Подключено»
- **Проверка:** лого видны без кликов

### A8. 🔲 Интеграции: привязка токена бота прямо в разделе
- **От юзера:** «в интеграциях нельзя добавить бота тг, не могу привязать токен»
- [ ] Карточка Telegram: если токена нет — inline-поле токена + «Подключить» (вызов updateBot c token) внутри Интеграций, не через Настройки
- [ ] После сохранения: карточка «Добавлен», появляется секция «Подключение: нажмите START в боте» с кнопкой «Открыть бота в Telegram» (botUrl) — та самая кнопка, которую юзер ждал
- [ ] При `mediaSyncDone` — бейдж «Синхронизирован»
- **Проверка:** ввести токен → сохранить → блок START появился → после START в боте — «Синхронизирован»

### A9. 🔲 Монетизация-поля переехали (касса+оферта уже в Интеграциях — верифицировать)
- [x] Касса: 3 карточки с полями и инструкцией «Где взять»
- [x] Оферта: поле + «Сохранить» прямо на странице
- [ ] Убрать двойной заголовок (PageHeader + внутренние дубли) — финальный визуальный проход
- **Gate:** lint/build

---

## ФАЗА B — Рассылки v2 (канвас 8e61bb02) и медиа

### B1. ✅ Бэкенд: медиа в рассылках — база
- ✅ Миграция `f1a2b3c4d5e6`: broadcasts.media_asset_ids (JSONB)
- ✅ Модель: media_asset_ids + text import
- ✅ broadcast_rq.create_broadcast: mediaAssetIds (валидация принадлежности боту, ≤10), text необязателен при наличии медиа
- ✅ broadcast_rq.get_broadcast_media: MediaAsset по порядку
- ✅ Схемы: mediaAssetIds в create/response
- [ ] API create endpoint передаёт mediaAssetIds (сделано в edit — проверить)
- [ ] Тесты pytest: создание с медиа, валидация ≤10 и чужих asset_id, text+media обязательность (одно из двух), scheduled с медиа

### B2. 🔲 Бэкенд: движок отправки медиа
- ✅ `_deliver_media_then_text`: 1 медиа → sendPhoto/sendVideo; 2-10 → send_media_group; затем текст отдельным сообщением
- ✅ run_broadcast_sending: грузит get_broadcast_media и передаёт в чанк
- [ ] Проверка Edge: у получателя с заблокированным ботом — failed на всю посылку (медиа+текст) — ок по ТЗ
- **Gate:** pytest 100+ passed

### B3. 🔲 Frontend композер: медиа-блок
- [ ] BroadcastComposerForm: секция «Фото и видео» — множественный выбор (input multiple, accept image/video), до 10 файлов, ≤20 МБ каждый
- [ ] Чипы-превью: миниатюра (object-url), тип, кнопка ✕; видео — иконка play
- [ ] Два режима:
  - бот привязан (mediaSyncDone) → загрузка сразу через `/api/bots/{id}/media?node_id=broadcast` → собираем mediaAssetIds
  - бот НЕ привязан → файлы хранятся на фронте (в памяти формы + IndexedDB `botflow_pending_media`), бейдж «Медиа прикрепится после привязки бота»
- [ ] После привязки бота (обзор рассылок): тихая доupload → assetId → обновить черновик-рассылку → удалить локальные копии
- [ ] Текст и медиа — раздельные сообщения: подпись UI «Медиа уйдёт отдельным сообщением, текст — следующим»
- [ ] Кнопка отправки: disabled, если медиа не загружены и бот не привязан
- **Gate:** lint/build, ручной тест юзера

### B4. 🔲 Двухколоночный макет вкладки «Рассылки» (после A5)
- [ ] Desktop: слева sticky конструктор (BroadcastComposerForm в карточке), справа лента со сводкой месяца (уже есть)
- [ ] Mobile: кнопка «+ Новая рассылка» → bottom-sheet с той же формой (уже есть)
- [ ] Убрать double-render экранов из A5
- **Gate:** скрины desktop+mobile

### B5. 🔲 Медиа в сценарии — несколько фото/видео на шаг

**Модель (схема воронки):**
- Узел получает `mediaAssets: [{ mediaFileId, mediaAssetId, mediaType }]` (массив до 10) вместо одиночного `media/mediaFileId/mediaAssetId/mediaType`
- Обратная совместимость: при загрузке старой воронки одиночные поля конвертируются в массив (frontend mapper)
- Бэкенд readiness: «медиа настроено» = len(mediaAssets) > 0

**Бэкенд отправка ботом:**
- 1 медиа → sendPhoto/sendVideo с текстом-подписью (лимит 1024)
- 2+ → sendMediaGroup (caption = текст, лимит 1024), если текст длиннее 1024 — текст отдельным сообщением после группы
- Затрагивает: обработчики шагов воронки у клиентского бота

**Фронт:**
- [ ] RichTextEditor → медиа-список: до 10 строк 72px (миниатюра+тип+Заменить/✕ каждая), кнопка «+ добавить»
- [ ] Хранение: bot привязан → upload (node_id=шаг) → file_id в массиве узла; НЕ привязан → локальные blob в IndexedDB (`botflow_pending_media`), бейдж «Загрузим после привязки», тихая доupload после привязки → удалить blob
- [ ] Лимит подписи шага: 1024 при медиа, 4096 без (сохранить), предупреждение «в медиа-группе подпись обрезается» при 2+ медиа
- [ ] Эмулятор: превью медиа-группы (2+ сетки подряд)

**Gate:** lint/build, pytest, smoke: 2 фото на Старт → сохранить → reload → на месте

---

## ФАЗА C — Подписка и Профиль

### C1. ✅ Подписка: hero → 3 равные карточки — `56a73ef`
- ✅ Цена 990 ₽/мес за каждого бота · Продление с датой · Автосписание (вкл/выкл + модал с реальной датой — c491434)
- ✅ «Боты в подписке» — список со статусами; per-bot checkout — после R6

### C2. ✅ Профиль: «Уведомления и чеки» — `31af560`
- ✅ Пояснение Telegram-уведомлений (неотключаемые), Email-поле с подсказкой, switch вместо чекбокса, формулировки без лицензий/PRO

---

## ФАЗА D — Скрины с картинками (IMAGE REQUEST по формату AGENTS.md)

### D1. 🔲 Слоты для супер-скринов (юзер генерит по промтам)
- [ ] Welcome: фоновая 3D-сцена за телефоном (опционально)
- [ ] Пустое состояние «Мои боты»: 3D-робот с ростком
- [ ] Пустое состояние «Рассылки»: 3D-мегафон с письмами
- [ ] Пустое состояние «Кассы»: 3D-касса с монетой
- [ ] PromoCard подписки: 3D- корона/звезда
- Каждый слот: точный промт → юзер генерит → кладём в frontend/public/visuals/...

### D2. 🔲 Админка — финал (после C)
- [ ] UsersSection/таблица DS v2
- [ ] Карточка пользователя: боты, подписки ботов, блокировка, платежи
- [ ] Платежи BotFlow: лента
- **Gate:** lint/build, скрины юзера

---

## ФАЗА E — релизная гигиена

- [ ] Убрать `<script src="/mock-api.js">` из index.html ПЕРЕД прод-деплоем (локальный тест остаётся в ветке)
- [ ] CONTEXT.md SESSION HANDOFF обновить
- [ ] Полный прогон: pytest, npm lint, npm build
- [ ] Ручной smoke в Telegram: Welcome при 0 ботов, создание бота, сценарий с медиа, публикация, рассылка (сейчас/запланирована/отмена), подписка
